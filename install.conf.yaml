- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.vimrc: .vimrc
    ~/.gitconfig: .gitconfig
    ~/.ssh/config: .ssh_config

- create:
    - ~/Downloads

- shell:
    - [git submodule update --init --recursive, Installing submodules]
